import React from 'react';

type Props = {
    children: React.ReactNode;
};

const Bob: React.FC<Props> = () => {
    return <></>;
};

const styles = {
    button: {},
};

export default Bob;
