export type SimilarityResponse = {
    similarity: number;
    responseTime: number;
};

export type LightLevelResponse = {
    lightLevel: number;
    responseTime: number;
};

export type BlurResponse = {
    degreeOfBlur: number;
    responseTime: number;
};
