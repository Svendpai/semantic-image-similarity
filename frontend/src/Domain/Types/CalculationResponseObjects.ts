export type SimilarityResponse = {
    similarity: number;
    responseTime: string;
};

export type LightLevelResponse = {
    lightLevel: number;
    responseTime: string;
};

export type BlurResponse = {
    degreeOfBlur: number;
    responseTime: string;
};
